{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Rogue Vita Desktop",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/rogue-vita-desktop",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "stopAtEntry": false
        }
    ]
}